<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.design.widget.TabLayout
        android:id="@+id/car_select_tabs"
        android:layout_width="match_parent"
        android:layout_height="@dimen/car_details_tab_height"
        app:tabMaxWidth="0dp"
        app:tabGravity="fill"
        android:background="@color/white"
        app:tabIndicatorColor="@color/disabled_text"
        app:tabTextColor="@color/disabled_text"
        app:tabIndicatorHeight="2dp"
        app:tabSelectedTextColor="#000"
        app:tabMode="fixed"/>

    <android.support.v4.view.ViewPager
        android:layout_below="@+id/car_select_tabs"
        android:id="@+id/car_select_view_pager"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />


</RelativeLayout>