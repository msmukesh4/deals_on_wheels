<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    // edit text style
    <style name="EditTextStyle" parent="@android:style/Widget.EditText">
        <item name="android:background">@drawable/edit_text_enable_disable</item>
        <item name="android:textColor">@color/ultra_dark</item>
        <item name="android:textColorHint">@color/edit_text_hints</item>
    </style>

    <style name="EditTextSingleLineStyle" parent="@style/EditTextStyle">
        <item name="android:singleLine">true</item>
    </style>

    // button style
    <style name="ButtonDanger" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/danger</item>
        <item name="colorControlHighlight">@color/danger_dark</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/button_text_size</item>
    </style>


    <style name="ButtonSuccess" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/success</item>
        <item name="colorControlHighlight">@color/success_dark</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/button_text_size</item>
    </style>

    <style name="ButtonPrimary" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/primary</item>
        <item name="colorControlHighlight">@color/primary_dark</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/button_text_size</item>
    </style>




</resources>
